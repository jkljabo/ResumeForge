ResumeForge Design Principles

• Favor composition over inheritance.

• Keep modules small.

• Business logic belongs outside the CLI.

• All external dependencies are wired in bootstrap.py.

• Prefer dependency injection.

• Tests should isolate collaborators.

• Every new feature should have automated tests.

• Preserve backward-compatible CLI behavior whenever practical.